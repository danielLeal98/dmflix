import styled from 'styled-components';

export const FooterBase = styled.footer`
  background: var(--black);
  border-top: none;
  padding-left: 16px;
  padding-right: 16px;
  color: var(--white);
  text-align: center;
  margin-top: auto;
`;
